import { ModalAddsPipe } from './modal-adds.pipe';

describe('ModalAddsPipe', () => {
  it('create an instance', () => {
    const pipe = new ModalAddsPipe();
    expect(pipe).toBeTruthy();
  });
});
